"""Shared account facts for the RemoteLink staging model.

Trimmed to what the `.xml` encoder path needs. (The former DBISAM/ODBC/DBSYS
apply config lived here too, but the `.sql` apply path was dropped in favour of
RemoteLink's own `.xml` import.)
"""
from __future__ import annotations

DEFAULT_PANEL_MODEL = "XR550"
DEFAULT_AREA = "01"
